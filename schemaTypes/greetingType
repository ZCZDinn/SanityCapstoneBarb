import {defineType} from 'sanity'

export const greetingType = defineType({
  name: 'greetingItem',
  title: 'Greeting Item',
  type: 'document',
  fields: [
    {
        title: 'Content', 
        name: 'content',
        type: 'array', 
        of: [{type: 'block'}]
    }
    ],
})